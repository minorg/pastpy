<!DOCTYPE html>
<html lang="en">

<head>
    {{>head_meta.html}} {{>head_css.html}}
    <title>{{metadata.configuration.name}}</title>
</head>

<body>
    <div class="container-fluid index top-level-container">
        {{>navbar.html}}
        <div class="first-row row"></div>
        <div class="row text-center w-100">
            <div class="col-12">
                {{#metadata.configuration.institution_name}}
                <h1>{{metadata.configuration.institution_name}}</h1>
                <h2>{{metadata.configuration.name}}</h2>
                {{/metadata.configuration.institution_name}} {{^metadata.configuration.institution_name}}
                <h1>{{metadata.configuration.name}}</h1>
                {{/metadata.configuration.institution_name}}
            </div>
        </div>
        <div class="row mt-2 text-center w-100">
            <div class="col-4 offset-4">
                <gcse:searchbox-only />
            </div>
        </div>
        {{#has_featured_searches}}
        <div class="row w-100 mt-4">
            <div class="col-12 text-center">
                <strong>Try:</strong>
            </div>
        </div>
        <div class="row">
            <div class="col-4 offset-4 text-center">
                <div class="container-fluid">
                    <div class="row">
                        {{#metadata.configuration.featured_searches}}
                        <div class="col-6">
                            <a href="https://cse.google.com/cse?cx={{{metadata.configuration.google_custom_search_engine_id}}}&q={{query}}">{{name}}</a>
                        </div>
                        {{/metadata.configuration.featured_searches}}
                    </div>
                </div>
            </div>
        </div>
        {{/has_featured_searches}}
        <div class="last-row row">
        </div>
    </div>
    {{>footer.html}} {{>scripts.html}}
</body>

</html>